console.log('The scrpit 2 is executing after loading');

const newVariableTwo = 'a new variable';

function newFunctionTwo(numberOfFunction) {
  console.log('New function is executing ' + numberOfFunction);
}
